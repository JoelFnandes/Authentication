import React from "react";
import "./button.css";

const STYLES = [
    'btn--primary',
    'btn-outline'
]

const SIZES = [
    'btn--medium',
    'btn--large'
]

export const Button = ({
    children,
    type,
    onclick,
    buttonStyle,
    buttonSize
})

